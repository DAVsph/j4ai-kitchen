import{NextResponse}from"next/server";export const runtime="nodejs";export async function POST(req:Request){try{if(!process.env.OPENAI_API_KEY)return NextResponse.json({error:"OPENAI_API_KEY non configurée"},{status:503});const{prompt}=await req.json();if(!prompt)return NextResponse.json({error:"Brief vide"},{status:400});const r=await fetch("https://api.openai.com/v1/images/generations",{method:"POST",headers:{Authorization:`Bearer ${process.env.OPENAI_API_KEY}`,"Content-Type":"application/json"},body:JSON.stringify({model:"gpt-image-1",prompt:`Rendu d'architecture intérieure photoréaliste haut de gamme. ${prompt}. Proportions crédibles, implantation réalisable, lumière naturelle, sans texte ni logo.`,size:"1024x1024",quality:"medium",n:1})});const d=await r.json();if(!r.ok)return NextResponse.json({error:d?.error?.message||"Erreur OpenAI"},{status:r.status});const x=d?.data?.[0];const image=x?.b64_json?`data:image/png;base64,${x.b64_json}`:x?.url;return NextResponse.json({image})}catch{return NextResponse.json({error:"Génération impossible"},{status:500})}}